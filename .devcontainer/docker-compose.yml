version: '3'

services:
  codestanding-ml:
    build:
      context: ..
      dockerfile: .devcontainer/dockerfile-gpu.dockerfile
    ports:
      - 8000:8000
    volumes:
      - ..:/workspace
    command: sleep infinity